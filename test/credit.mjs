import { describe, it } from 'node:test';

describe('credit transaction', () => {
  it('should return the amount add to the balance', () => {

  })

  it('should return the amount not added if 1000 of the balance is reached', () => {

  });

  it('should return the new balance', () => {
      
  });
});