import { describe, it } from 'node:test';
describe('debit transaction', () => {
  it('should return the amount subtracted from the balance', () => {
      
  });

  it('should return the amount not subtracted if the balance is 0', () => {
        
  });
  
  it('should return the new balance', () => {
      
  });
});