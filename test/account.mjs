import { describe, it } from 'node:test'
import { doesNotThrow, throws } from 'node:assert';

describe('account', () => {
  it('should have a unique label', () => {

  });
  it('should have a balance between 0 and 1000', () => {

  });
});